print("-------------")
for i in range (1, 6):
    c = 6-i
    w = 5*i
    r = [str(w)]*c # создаётся список из одного элемента, где число преобразовано в строку;
    print(" ".join(r)) #объединяет элементы списка r в одну строку, разделяя их пробелом;

