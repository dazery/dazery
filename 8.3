import random

a = random.randint(0, 1)
print(a)

if a < 0 or a > 1:
    print("a не в диапазоне от 0 до 1 включительно.")
else:
    k = 1
    r = 1 / k

    while r >= a:
        print(f"1/{k} = {r:.6f}")
        k += 1
        r = 1 / k
